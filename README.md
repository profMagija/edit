# `edit` - a tiny text editor

To use:

```
edit [filename]
```

- `Ctrl-C` exit
- `Ctrl-S` save
- `Ctrl-K` enter command mode. Command currently implemented:
  - `save [filename]` save as given filename
  
That's it.